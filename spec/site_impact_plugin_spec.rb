# frozen_string_literal: true

RSpec.describe SiteImpactPlugin do
  it "has a version number" do
    expect(SiteImpactPlugin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
